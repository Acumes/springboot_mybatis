<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.htf.mapper.MeasureunitTypeMapper">
    <!--开启二级缓存-->
    <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>
        <!--根据id查询用户详情-->
    <sql id="Base_Column_List" >
      measureunit_type_code code, measureunit_type_name name,sort_seq sortSeq,status,comment
    </sql>
    <!--根据id查询用户详情-->
    <select id="selectByPrimaryKey" parameterType="string" resultType="com.htf.entity.MeasureunitType">
        SELECT 
        <include refid="Base_Column_List" />
        FROM measureunit_type WHERE id=#{id}
    </select>
    <update id="updateByPrimaryKey" parameterType="com.htf.entity.Menu">
        update MENU
        <set>
            <if test="name != null">
                NAME = #{name,jdbcType=VARCHAR},
            </if>
            <if test="title != null">
                TITLE = #{title,jdbcType=TINYINT},
            </if>
            <if test="parentId != null">
                PARENTID = #{parentId,jdbcType=VARCHAR},
            </if>
            <if test="type != null">
                TYPE = #{type,jdbcType=VARCHAR},
            </if>
            <if test="modifier != null">
                MODIFIER = #{modifier,jdbcType=TINYINT},
            </if>
            MODIFY_DATE = SYSDATE()
        </set>
        where ID = #{id,jdbcType=VARCHAR}
    </update>
    <insert id="insert" parameterType="com.htf.entity.Menu">
        insert into MENU (ID, NAME,PARENTID,
        TITLE,`ORDER`,TYPE,
        CREATE_DATE, CREATOR)
        values (#{id,jdbcType=VARCHAR}, #{name, jdbcType=VARCHAR},
        #{parentId,jdbcType=VARCHAR}, #{title,jdbcType=VARCHAR}, #{order,jdbcType=VARCHAR},
        #{type,jdbcType=VARCHAR},SYSDATE(), #{creator,jdbcType=VARCHAR})
    </insert>
    <delete id="deleteByPrimaryKey">
        DELETE  FROM MENU WHERE id = #{id,jdbcType=VARCHAR}
    </delete>
    <select id="selectMeasureunitList"  resultType="com.htf.entity.MeasureunitType">
        SELECT
        <include refid="Base_Column_List" />
        FROM measureunit_type WHERE status='1'
    </select>
</mapper>